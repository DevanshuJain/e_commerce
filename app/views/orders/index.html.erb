<table class="table">
  <thead>
  	<tr>
	  <th>order_id</th>
	  <th>amount</th>
	  <th>status</th>
  	</tr>
  </thead>
  <tbody>
  	<% @orders.each do |order| %>
      <tr>
	    <th><%= order.id %></th>
	    <th><%= order.total_ammount %></th>
	    <th> Done </th>
	  </tr>
    <% end %>
  </tbody>
</table>
<p><%= link_to 'back', products_path %></p>
